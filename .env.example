# ============================================================
# ARCCO AI — Variáveis de Ambiente (OPCIONAL)
#
# Este arquivo é apenas para referência ou para sobrescrever
# configurações em ambientes diferentes.
#
# ⚠️  NA VPS: NÃO É NECESSÁRIO CRIAR ESTE ARQUIVO.
#     O backend usa as credenciais do Supabase hardcoded em
#     backend/core/config.py e carrega todas as API keys
#     dinamicamente da tabela ApiKeys no Supabase.
#
# Para ativar a busca web:
#   → Abra o painel do Supabase
#   → Vá em Table Editor > ApiKeys
#   → Insira a chave Tavily (ou Brave) com is_active=true:
#     { provider: "tavily", api_key: "tvly-...", is_active: true }
#
# ============================================================

# ── Supabase (já configurado no código — só preencha para sobrescrever) ──
# SUPABASE_URL=https://seu-projeto.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
# SUPABASE_STORAGE_BUCKET=chat-uploads

# ── API Keys (preferir configurar via Supabase ApiKeys table) ────────────
# OPENROUTER_API_KEY=sk-or-...
# TAVILY_API_KEY=tvly-...
# BRAVE_SEARCH_API_KEY=BSA...

# ── Modelos (opcionais — têm defaults) ──────────────────────────────────
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
# AGENT_MODEL=claude-sonnet-4-5-20250929
# AGENT_MAX_TOKENS=8096
# AGENT_MAX_ITERATIONS=20

# ── Outros (opcionais) ───────────────────────────────────────────────────
# ALLOW_CODE_EXEC=false
# CORS_ORIGINS=*
# LOG_LEVEL=INFO
